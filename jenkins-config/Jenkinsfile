pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Build step"
            }
        }

        stage('Test') {
            steps {
                echo "Running tests..."
                echo "Tests passed successfully"
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploy project"
            }
        }
    }
}
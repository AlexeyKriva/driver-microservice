pipeline {
    agent {
        docker {
            image 'maven:latest'
            args '-u root -v $HOME/.m2:/root/.m2'
        }
    }

    stages {
        stage('Build') {
            steps {
                echo "Building project..."
                sh 'mvn clean install -DskipTests'
            }
        }

        stage('Linter') {
            steps {
                echo "Linter"
                sh 'mvn checkstyle:check -Dcheckstyle.consoleOutput=true'
            }
        }

        stage('Test') {
            steps {
                echo "Running tests..."
                sh 'mvn test'
            }
        }

        stage('Sonarqube') {
            steps {
                echo "Sonarqube working..."
                sh 'curl -v http://host.docker.internal:9000'
            }
        }
    }
}
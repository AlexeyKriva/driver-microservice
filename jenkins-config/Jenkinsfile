pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "Build step"'
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Running tests..."'
                sh 'echo "Tests passed successfully"'
            }
        }

        stage('Deploy') {
            steps {
                sh 'echo "Deploy project"'
            }
        }
    }
}